{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "cd clientApp && npm install --legacy-peer-deps && npm run build & cd ..",
    "start": "cd clientApp && npm install --legacy-peer-deps && npm run build & cd .. & node app.js",
    "heroku-postbuild": "cd clientApp && npm install --legacy-peer-deps && npm run build"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^16.0.3",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "immutable": "^4.2.4",
    "jade": "~1.11.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.7.2",
    "morgan": "~1.9.1",
    "nodemon": "^2.0.20"
  }
}
